# include <stdio.h>
# include <stdlib.h>

int main() {
	int k[4];
	for (int i = 0; i < 4; i++)
	{
		scanf_s("%d", &k[i]);
	}
	int max = k[0];
	for (int j = 0; j < 4; j++)
	{
		if (max < k[j])
		{
			max = k[j];
		}
	}
	printf("The max number is %d", max);
	system("pause");
}
